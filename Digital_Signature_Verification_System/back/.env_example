# ======================
# Supabase 설정
# ======================
SUPABASE_URL="https://your-project-ref.supabase.co"
SUPABASE_KEY="your-supabase-anon-key"

# ======================
# 디지털 서명 설정
# ======================

# RSA-2048 개인 키 (PEM 형식)
# ⚠️ 반드시 한 줄로 입력하며, 줄바꿈은 \n 로 대체하세요.
# openssl genpkey -algorithm RSA -out private_key.pem -pkeyopt rsa_keygen_bits:2048
# cat private_key.pem | awk 'BEGIN{ORS="\\n"} {print}'  # 줄바꿈 변환 예시
ISSUING_AUTHORITY_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_CONTENT\n-----END PRIVATE KEY-----"

# ======================
# JWT 설정
# ======================
# 강력한 랜덤 키 생성 예시: openssl rand -hex 32
SECRET_KEY="your-secret-key"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ======================
# 프론트엔드 연동 설정
# ======================

# Sentry DSN (선택 사항)
VITE_SENTRY_DSN_FRONTEND="https://...@o123.ingest.sentry.io/789"

# API 서버 URL
VITE_API_BASE_URL="http://localhost:8000"